from fastapi import APIRouter

router = APIRouter()

# main.py から移動したエンドポイント定義をここに貼り付け
# 例: @router.post("/chatbot/api/chat", response_model=ChatResponse)
# async def chat(...):
#     ...

# 全てのエンドポイントを @router で定義 